[project]
name = "my_ai_assistant"
version = "0.0.1"
description = "Small assistant package"
requires-python = ">=3.11"

[tool.poetry.dependencies]
python = ">=3.11"
openai = "*"
python-dotenv = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
